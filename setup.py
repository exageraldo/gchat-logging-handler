from setuptools import setup


# Metadata goes in setup.cfg. These are here for GitHub's dependency graph.
setup(
    name="gchat_logging_handler",
    version="0.0.1-dev",
    install_requires=[
        "httpx==0.22.0"
    ]
)
